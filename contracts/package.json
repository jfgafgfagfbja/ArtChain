{
  "name": "artchain-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "ArtChain smart contracts (Hardhat)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "demo:verify": "hardhat run scripts/demo-verify.js",
    "demo:register": "hardhat run scripts/demo-register.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.29.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
